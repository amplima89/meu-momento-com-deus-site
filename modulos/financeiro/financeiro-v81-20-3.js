"use strict";
window.MemoryFinanceSimple=(()=>{
const STORE_KEY="financeiro_criptografado_v1",LOCAL_KEY="memory:financeiro:encrypted:v1",PASSWORD_HASH="59e66102621bc60648811c309f8475f58628b86c8a11176cb9003c79b165ee96";
const enc=new TextEncoder(),dec=new TextDecoder();let state=null,password="",saving=Promise.resolve(),syncMode="";
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];const uid=p=>`${p}_${Date.now().toString(36)}_${Math.random().toString(36).slice(2,8)}`;const num=v=>Number.isFinite(Number(v))?Number(v):0;const brl=v=>num(v).toLocaleString("pt-BR",{style:"currency",currency:"BRL"});
const norm=v=>String(v||"").normalize("NFD").replace(/[\u0300-\u036f]/g,"").toUpperCase().replace(/[^A-Z0-9]+/g," ").replace(/\s+/g," ").trim();
const period=()=>$("#finance-period")?.value||new Date().toISOString().slice(0,7);const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const DEFAULT_LINES=[
 ["cartao-outros","Cartão de crédito","expense",2396.28],["combustivel","Combustível","expense",453.03],["livros","Livros","expense",150],["seguro-carro","Seguro carro","expense",278.04],["racao","Ração","expense",400],["gympass","Gympass","expense",139.90],["barbeiro","Barbeiro","expense",139.99],["gas","Gás","expense",0],["iptu","IPTU","expense",0],["celular","Celular","expense",51.75],["luz-cpfl","Luz CPFL","expense",113.60],["internet","Internet","expense",199.99],["condominio","Condomínio","expense",0],["agua","Água","expense",48.70],["moradia","Casa alugada/própria","expense",3330],["outras","Outras despesas","expense",2200],["amortizacao","Amortização casa","expense",0],["renda-taina","Tainá","income",1100],["salario","Líquido Salário","income",12770.53],["ppr","PPR","income",0],["13-salario","13º salário","income",0]
];
const BASE_RULES=[
 ["POSTO","combustivel"],["COMBUST","combustivel"],["WELLHUB","gympass"],["GYMPASS","gympass"],["BARBER","barbeiro"],["BARBE","barbeiro"],["ZURICH","seguro-carro"],["SEGURO","seguro-carro"],["LIVRARIA","livros"],["LIVRO","livros"],["AMAZON","livros"],["PET","racao"],["RACAO","racao"],["SAAE","agua"],["CPFL","luz-cpfl"],["NETTOP","internet"],["INTERNET","internet"],["CLARO","celular"],["CONDOMIN","condominio"],["ALUGUEL","moradia"],["SALARIO","salario"],["PPR","ppr"]
];
function defaultState(){const p=period();return{version:6,accounts:DEFAULT_LINES.map(([id,name,type,value])=>({id,name,type,active:true,plannedMonthly:true,amounts:{[p]:value}})),rules:BASE_RULES.map(([keyword,accountId])=>({keyword,accountId})),transactions:[],files:[],months:{},updatedAt:new Date().toISOString()}}
async function sha256(v){const d=await crypto.subtle.digest("SHA-256",enc.encode(v));return[...new Uint8Array(d)].map(b=>b.toString(16).padStart(2,"0")).join("")}
function b64(b){let s="";for(let i=0;i<b.length;i+=32768)s+=String.fromCharCode(...b.subarray(i,i+32768));return btoa(s)}function unb64(v){return Uint8Array.from(atob(v),c=>c.charCodeAt(0))}
async function key(secret,userId){const m=await crypto.subtle.importKey("raw",enc.encode(secret),"PBKDF2",false,["deriveKey"]);return crypto.subtle.deriveKey({name:"PBKDF2",salt:enc.encode(`memory-financeiro-v1:${userId}`),iterations:150000,hash:"SHA-256"},m,{name:"AES-GCM",length:256},false,["encrypt","decrypt"])}
async function encrypt(v,secret){const u=await MMCDAuth.user(),k=await key(secret,u.id),iv=crypto.getRandomValues(new Uint8Array(12)),plain=enc.encode(JSON.stringify(v)),cipher=await crypto.subtle.encrypt({name:"AES-GCM",iv},k,plain);return{v:1,alg:"AES-GCM+PBKDF2",iv:b64(iv),data:b64(new Uint8Array(cipher)),updatedAt:new Date().toISOString()}}
async function decrypt(e,secret){const u=await MMCDAuth.user(),k=await key(secret,u.id),plain=await crypto.subtle.decrypt({name:"AES-GCM",iv:unb64(e.iv)},k,unb64(e.data));return JSON.parse(dec.decode(plain))}
async function readEnvelope(){try{const l=JSON.parse(localStorage.getItem(LOCAL_KEY)||"null");if(l?.data){syncMode="Local criptografado";return l}}catch{}try{const r=await MemoryConfig.read(STORE_KEY,null,{fresh:true});if(r?.data){localStorage.setItem(LOCAL_KEY,JSON.stringify(r));syncMode="Supabase criptografado";return r}}catch(e){console.warn(e)}return null}
async function persist(){if(!state||!password)return;state.updatedAt=new Date().toISOString();const e=await encrypt(state,password);localStorage.setItem(LOCAL_KEY,JSON.stringify(e));try{await MemoryConfig.write(STORE_KEY,e);syncMode="Supabase criptografado"}catch(err){syncMode="Local criptografado";console.warn(err)}updateSync()}
function save(){saving=saving.catch(()=>{}).then(persist);return saving}
function migrate(){state ||= defaultState();state.version=6;state.accounts=Array.isArray(state.accounts)?state.accounts:[];state.transactions=Array.isArray(state.transactions)?state.transactions:[];state.files=Array.isArray(state.files)?state.files:[];state.rules=Array.isArray(state.rules)?state.rules:[];
 // preserve old model, but make only genuine monthly lines visible
 state.accounts.forEach(a=>{a.amounts ||= {};if(a.type==='settlement'||a.classificationOnly)a.active=false});
 DEFAULT_LINES.forEach(([id,name,type,value])=>{let a=state.accounts.find(x=>x.id===id);if(!a){a={id,name,type,active:true,plannedMonthly:true,amounts:{}};state.accounts.push(a)}a.name=a.name||name;a.type=a.type||type;a.active=a.active!==false;a.plannedMonthly=true;if(a.amounts[period()]===undefined&&Object.keys(a.amounts).length===0)a.amounts[period()]=value});
 BASE_RULES.forEach(([keyword,accountId])=>{if(!state.rules.some(r=>norm(r.keyword)===keyword&&r.accountId===accountId))state.rules.push({keyword,accountId})})
}
async function unlock(secret){secret=String(secret||"").trim();if(!secret)throw Error("Digite a senha.");if(await sha256(secret)!==PASSWORD_HASH)throw Error("Senha incorreta.");password=secret;const e=await readEnvelope();state=e?await decrypt(e,secret):defaultState();migrate();await save();}
function updateSync(){const b=$("#finance-sync-badge");if(!b)return;b.hidden=false;b.textContent=syncMode||"Protegido"}
function lines(){return state.accounts.filter(a=>a.active!==false&&a.plannedMonthly!==false&&a.type!=="settlement"&&!a.classificationOnly)}
function amount(a,p=period()){return Math.max(0,num(a.amounts?.[p]))}
function txs(p=period()){return state.transactions.filter(t=>t.period===p&&!t.ignored)}
function actualFor(account){return txs().filter(t=>t.accountId===account.id).reduce((s,t)=>s+Math.abs(num(t.amount)),0)}
function statusFor(a){const b=amount(a),r=actualFor(a);if(a.type==='income'){if(!r)return['Pendente','warn'];return[r+0.01>=b?'✓ Recebido':'Parcial',r+0.01>=b?'ok':'warn']}if(!r)return['Pendente','warn'];if(b<=0)return['Realizado','ok'];if(r<=b+0.01)return['✓ Dentro','ok'];return['Acima','over']}
function renderBudget(){const body=$("#budget-body"),ls=lines();body.innerHTML=ls.map(a=>{const r=actualFor(a),[st,cl]=statusFor(a);return`<tr data-id="${esc(a.id)}"><td><input data-name value="${esc(a.name)}"></td><td><select data-type><option value="expense" ${a.type==='expense'?'selected':''}>Despesa</option><option value="income" ${a.type==='income'?'selected':''}>Receita</option></select></td><td><input data-budget type="number" min="0" step="0.01" value="${amount(a).toFixed(2)}"></td><td class="finance-money"><strong>${brl(r)}</strong></td><td><span class="finance-status ${cl}">${st}</span></td><td><button class="finance-delete" data-delete title="Excluir">🗑️</button></td></tr>`}).join('');$("#budget-empty").hidden=!!ls.length;
 body.querySelectorAll('tr').forEach(tr=>{const a=state.accounts.find(x=>x.id===tr.dataset.id);tr.querySelector('[data-name]').addEventListener('change',e=>{a.name=e.target.value.trim()||a.name;save();renderAll()});tr.querySelector('[data-type]').addEventListener('change',e=>{a.type=e.target.value;save();renderAll()});tr.querySelector('[data-budget]').addEventListener('change',e=>{a.amounts||={};a.amounts[period()]=Math.max(0,num(e.target.value));save();renderAll()});tr.querySelector('[data-delete]').addEventListener('click',()=>{if(confirm(`Excluir "${a.name}" deste controle?`)){a.active=false;save();renderAll()}})});
 const expense=ls.filter(a=>a.type==='expense'),income=ls.filter(a=>a.type==='income');const b=expense.reduce((s,a)=>s+amount(a),0),r=expense.reduce((s,a)=>s+actualFor(a),0);$("#sum-budget").textContent=brl(b);$("#sum-actual").textContent=brl(r);$("#sum-diff").textContent=brl(b-r)}
function renderImports(){const list=txs().sort((a,b)=>(a.date||'').localeCompare(b.date||''));$("#imports-empty").hidden=!!list.length;$("#imports-body").innerHTML=list.map(t=>{const a=state.accounts.find(x=>x.id===t.accountId);return`<tr><td><span class="finance-source ${t.source==='invoice'?'card':'bank'}">${t.source==='invoice'?'💳 Fatura':'🏦 Extrato'}</span></td><td>${esc(formatDate(t.date))}</td><td>${esc(t.description)}</td><td class="finance-money">${brl(Math.abs(t.amount))}</td><td>${esc(a?.name||'—')}</td><td>${t.matched?'<span class="finance-check">✓ Orçado</span>':'<span class="finance-auto">Automático</span>'}</td></tr>`}).join('');$("#finance-pending-count").textContent=list.filter(t=>!t.matched).length}
function renderAll(){renderBudget();renderImports();updateSync()}
function formatDate(v){if(!v)return'—';try{return new Date(v+'T12:00:00').toLocaleDateString('pt-BR')}catch{return v}}
function parseDate(v){if(v instanceof Date&&!isNaN(v))return v.toISOString().slice(0,10);if(typeof v==='number'&&window.XLSX?.SSF?.parse_date_code){const d=XLSX.SSF.parse_date_code(v);if(d)return`${d.y}-${String(d.m).padStart(2,'0')}-${String(d.d).padStart(2,'0')}`}const s=String(v||'').trim();let m=s.match(/^(\d{1,2})[\/-](\d{1,2})[\/-](\d{2,4})$/);if(m){let y=+m[3];if(y<100)y+=2000;return`${y}-${m[2].padStart(2,'0')}-${m[1].padStart(2,'0')}`}m=s.match(/^(\d{4})-(\d{2})-(\d{2})/);return m?`${m[1]}-${m[2]}-${m[3]}`:''}
function money(v){if(typeof v==='number')return v;let s=String(v??'').trim().replace(/R\$\s*/gi,'').replace(/\s/g,'');if(!s)return NaN;if(/^-?[\d.]+,\d{1,2}$/.test(s))return Number(s.replace(/\./g,'').replace(',','.'));if(/^-?\d+(?:\.\d+)?$/.test(s))return Number(s);return NaN}
function accountById(id){return state.accounts.find(a=>a.id===id&&a.active!==false)}
function fallback(source,amount){if(source==='invoice'){let a=accountById('cartao-outros');if(!a){a={id:'cartao-outros',name:'Cartão de crédito',type:'expense',active:true,plannedMonthly:true,amounts:{}};state.accounts.push(a)}return a}if(amount>0){let a=accountById('outras-receitas');if(!a){a={id:'outras-receitas',name:'Outras receitas',type:'income',active:true,plannedMonthly:true,amounts:{}};state.accounts.push(a)}return a}let a=accountById('outras');if(!a){a={id:'outras',name:'Outras despesas',type:'expense',active:true,plannedMonthly:true,amounts:{}};state.accounts.push(a)}return a}
function classify(description,source,amount){const n=norm(description),candidates=lines();for(const r of state.rules){if(r.keyword&&n.includes(norm(r.keyword))){const a=accountById(r.accountId);if(a)return{account:a,matched:true}}}for(const a of candidates){const an=norm(a.name);if(an.length>=4&&(n.includes(an)||an.split(' ').filter(x=>x.length>=4).some(x=>n.includes(x))))return{account:a,matched:true}}return{account:fallback(source,amount),matched:false}}
function detectRows(rows,source){const out=[];for(const row of rows){const vals=Object.values(row||{});let date='',description='',value=NaN;for(const [k,v] of Object.entries(row||{})){const nk=norm(k);if(!date&&(nk.includes('DATA')||nk.includes('DATE')))date=parseDate(v);if(!description&&(nk.includes('DESCR')||nk.includes('HISTOR')||nk.includes('ESTAB')||nk.includes('LANCAMENTO')||nk.includes('LANÇAMENTO')))description=String(v??'').trim();if(!Number.isFinite(value)&&(nk.includes('VALOR')||nk.includes('AMOUNT')||nk.includes('TOTAL')))value=money(v)}
 if(!date){for(const v of vals){const d=parseDate(v);if(d){date=d;break}}}if(!description){description=vals.find(v=>typeof v==='string'&&v.trim().length>=3&&!parseDate(v))?.trim()||''}if(!Number.isFinite(value)){const nums=vals.map(money).filter(Number.isFinite);if(nums.length)value=nums[nums.length-1]}
 if(description&&Number.isFinite(value)&&Math.abs(value)>0){if(source==='invoice')value=-Math.abs(value);out.push({date,description,amount:value})}}
 return out}
async function importFile(file,source){const ab=await file.arrayBuffer(),wb=XLSX.read(ab,{type:'array',cellDates:true}),ws=wb.Sheets[wb.SheetNames[0]],rows=XLSX.utils.sheet_to_json(ws,{defval:'',raw:true});const detected=detectRows(rows,source);if(!detected.length)throw Error('Não encontrei lançamentos reconhecíveis nesse arquivo.');const p=period();let matched=0;for(const item of detected){const c=classify(item.description,source,item.amount);state.transactions.push({id:uid('tx'),source,period:p,date:item.date||`${p}-01`,description:item.description,amount:item.amount,accountId:c.account.id,matched:c.matched,importedAt:new Date().toISOString(),fileName:file.name});if(c.matched)matched++}state.files.push({id:uid('file'),period:p,source,name:file.name,count:detected.length,importedAt:new Date().toISOString()});await save();renderAll();$("#import-message").textContent=`${file.name}: ${detected.length} lançamento(s). ${matched} encontrado(s) no orçamento; ${detected.length-matched} classificado(s) automaticamente.`}
function openAdd(){const m=$("#finance-modal");m.hidden=false;m.innerHTML=`<div class="finance-dialog"><h2>Incluir linha</h2><div class="finance-dialog-grid"><label>Descrição<input id="new-name" type="text" placeholder="Ex.: Escola"></label><label>Tipo<select id="new-type"><option value="expense">Despesa</option><option value="income">Receita</option></select></label><label>Orçado<input id="new-value" type="number" min="0" step="0.01" value="0"></label></div><div class="finance-dialog-actions"><button class="btn" id="new-cancel">Cancelar</button><button class="btn primary" id="new-save">Adicionar</button></div></div>`;$("#new-cancel").onclick=()=>m.hidden=true;$("#new-save").onclick=()=>{const name=$("#new-name").value.trim();if(!name)return;state.accounts.push({id:uid('line'),name,type:$("#new-type").value,active:true,plannedMonthly:true,amounts:{[period()]:Math.max(0,num($("#new-value").value))}});m.hidden=true;save();renderAll()}}
function bind(){
 $$("[data-tab]").forEach(b=>b.onclick=()=>{$$("[data-tab]").forEach(x=>x.classList.toggle('active',x===b));$$('[data-panel]').forEach(p=>p.classList.toggle('active',p.dataset.panel===b.dataset.tab))});
 $("#finance-period").onchange=()=>{localStorage.setItem('memory:financeiro:period',period());renderAll()};$("#add-budget-line").onclick=openAdd;
 $("#invoice-input").onchange=async e=>{try{if(e.target.files[0])await importFile(e.target.files[0],'invoice')}catch(err){$("#import-message").textContent=err.message}e.target.value=''};$("#bank-input").onchange=async e=>{try{if(e.target.files[0])await importFile(e.target.files[0],'bank')}catch(err){$("#import-message").textContent=err.message}e.target.value=''};
 $("#clear-period-imports").onclick=()=>{if(confirm('Remover os lançamentos importados deste mês?')){state.transactions=state.transactions.filter(t=>t.period!==period());state.files=state.files.filter(f=>f.period!==period());save();renderAll()}};
 $("#finance-lock-button").onclick=()=>location.reload();$("#finance-password-toggle").onclick=()=>{const i=$("#finance-password");i.type=i.type==='password'?'text':'password'};
 $("#finance-lock-form").onsubmit=async e=>{e.preventDefault();const btn=e.submitter,err=$("#finance-lock-error");err.textContent='';btn.disabled=true;try{await unlock($("#finance-password").value);$("#finance-lock").hidden=true;$("#finance-app").hidden=false;$("#finance-lock-button").hidden=false;renderAll()}catch(x){err.textContent=x.message||'Não foi possível abrir.'}finally{btn.disabled=false}}
}
async function init(){if(!window.MMCDAuth?.requireSession)throw Error('Autenticacao do Memory indisponivel.');await MMCDAuth.requireSession();$("#finance-period").value=localStorage.getItem('memory:financeiro:period')||new Date().toISOString().slice(0,7);bind();$("#finance-password")?.focus()}
return{init};})();
window.addEventListener('load',()=>MemoryFinanceSimple.init().catch(console.error));
